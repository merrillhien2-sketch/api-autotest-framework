# 使用文档

本文档详细介绍接口自动化测试框架的设计思路、各模块职责、数据驱动用法、扩展方式与常见问题。

---

## 一、整体架构

框架采用分层设计，职责清晰：

```
┌──────────────────────────────────────────────┐
│  testcases/  测试用例层（参数化 + 数据驱动）   │
├──────────────────────────────────────────────┤
│  common/     通用能力层                        │
│    http_client  封装 requests.Session          │
│    assertions   响应断言助手                   │
│    auth         登录鉴权                       │
│    logger       日志单例                       │
├──────────────────────────────────────────────┤
│  utils/      工具层                            │
│    extract      JSONPath / 正则提取            │
│    generator    随机测试数据                   │
│    db_checker   数据库校验占位                 │
├──────────────────────────────────────────────┤
│  config/     配置层（.env → pydantic-settings）│
├──────────────────────────────────────────────┤
│  mock_server/  内置被测服务（FastAPI）         │
├──────────────────────────────────────────────┤
│  conftest.py  全局 fixture + 服务生命周期      │
└──────────────────────────────────────────────┘
```

测试执行流程：

1. `conftest.py` 在 session 级用 uvicorn 后台线程启动 Mock 服务（随机端口）；
2. 探活 `/api/health` 就绪后，session 级登录获取 token；
3. 各用例通过 `api_client`（带 token）或 `no_auth_client`（无 token）发起请求；
4. 用 `common/assertions.py` 做断言，`loguru` 记录全过程日志；
5. 测试结束生成 `reports/report.html`，关闭 Mock 服务。

---

## 二、各模块详解

### 2.1 配置层 `config/`

- `settings.py`：使用 `pydantic-settings` 从 `.env` 与环境变量读取配置，
  通过 `get_settings()` 获取单例（`lru_cache` 缓存）。
- `logging_conf.py`：配置 loguru，控制台彩色输出 + 文件按天轮转（保留 7 天）。

### 2.2 通用层 `common/`

- `http_client.py`：`HttpClient` 封装 `requests.Session`：
  - 构造时挂载带 `urllib3.Retry` 的 `HTTPAdapter`，对幂等请求自动重试；
  - `request()` 统一记录请求/响应日志，自动注入 `Authorization: Bearer <token>`；
  - 提供 `get/post/put/delete/patch` 便捷方法与 `update_token/close`。
- `assertions.py`：语义化断言函数：
  - `assert_status_code` / `assert_status_in`：状态码断言；
  - `assert_json_field` / `assert_field_exists`：字段断言；
  - `assert_jsonpath`：JSONPath 取值断言（支持相等 / 包含）；
  - `assert_response_time`：响应耗时断言；
  - `assert_business_code` / `assert_message_contains`：业务码与消息断言。
- `auth.py`：`AuthHelper` 调用 `/api/login` 获取 token，提供 `auth_headers()`。
- `logger.py`：导出配置好的 `logger` 单例，业务代码 `from common.logger import logger`。

### 2.3 工具层 `utils/`

- `extract.py`：
  - `extract_jsonpath(data, expr)`：轻量 JSONPath，支持 `$.a.b`、`$.a[0]`、
    `$.a[*]`、`$..key`；
  - `extract_regex(text, pattern, group)`：正则提取。
- `generator.py`：随机用户名、邮箱、手机号、密码、文章标题等，保证用例数据唯一。
- `db_checker.py`：数据库校验占位，真实项目可在此接入 SQL 断言。

### 2.4 被测服务 `mock_server/`

`app.py` 基于 FastAPI 实现：

| 接口 | 方法 | 说明 | 鉴权 |
| --- | --- | --- | --- |
| `/api/health` | GET | 健康检查 | 否 |
| `/api/login` | POST | 账号密码登录，返回 JWT | 否 |
| `/api/articles` | GET | 文章列表（分页/关键字） | 是 |
| `/api/articles` | POST | 创建文章 | 是 |
| `/api/articles/{id}` | GET/PUT/DELETE | 查/改/删文章 | 是 |
| `/api/users` | GET | 用户列表（关键字） | 是 |
| `/api/users` | POST | 创建用户 | 是 |
| `/api/users/{username}` | GET/PUT/DELETE | 查/改/删用户 | 是 |

- 统一响应格式：`{"code": 0, "message": "success", "data": {...}}`；
- 业务码：`0` 成功，`1001` 登录失败，`1002` 鉴权失败，`1003` 文章不存在，
  `1004` 用户名已存在，`1005` 用户不存在，`1006` 禁止删除自身，`422` 参数校验失败；
- JWT(HS256) 使用标准库自行实现，无需额外依赖；
- 全局异常处理器统一错误响应格式；
- `data.db` 为运行时占位产物。

---

## 三、数据驱动用法

### 3.1 YAML 数据驱动

以 `data/login_data.yaml` 为例：

```yaml
testcases:
  - case_id: login_001
    name: 正确账号密码登录成功
    username: admin
    password: "123456"
    expect_status: 200
    expect_code: 0
    expect_jsonpath: "$.data.token"
```

在测试中加载并参数化：

```python
import yaml
from pathlib import Path

DATA_FILE = Path(__file__).resolve().parent.parent / "data" / "login_data.yaml"

def _load_cases():
    with open(DATA_FILE, encoding="utf-8") as f:
        return yaml.safe_load(f)["testcases"]

_CASES = _load_cases()

@pytest.mark.parametrize("case", _CASES, ids=[c["case_id"] for c in _CASES])
def test_login(case, no_auth_client):
    ...
```

### 3.2 CSV 数据驱动（pandas）

`data/users.csv`：

```csv
case_id,username,email,nickname,expect_status,expect_code
user_001,user_{rand},alice@autotest.com,爱丽丝,200,0
```

`{rand}` 占位符在加载时替换为随机串，保证每次运行用户名唯一：

```python
import pandas as pd

def _load_csv_cases():
    df = pd.read_csv(CSV_FILE, dtype=str)
    cases = []
    for _, row in df.iterrows():
        case = {col: str(row[col]) for col in df.columns}
        case["username"] = case["username"].replace("{rand}", random_string(6))
        cases.append(case)
    return cases
```

### 3.3 新增数据驱动用例

1. 在 `data/` 下新增 YAML 或 CSV 文件；
2. 在 `testcases/` 下新增测试文件，加载数据并 `parametrize`；
3. 使用 `api_client` fixture 发请求、`assertions` 断言即可。

---

## 四、Fixture 说明

`conftest.py` 提供以下 fixture：

| Fixture | 作用域 | 说明 |
| --- | --- | --- |
| `mock_server` | session | 启动 Mock 服务，返回 base_url |
| `base_url` | session | 被测服务地址 |
| `auth_token` | session | 登录后的 token |
| `api_client` | function | 带 token 的 HttpClient（每用例独立） |
| `no_auth_client` | function | 不带 token 的 HttpClient |
| `data_dir` | session | 数据文件目录路径 |

---

## 五、对接真实被测服务

框架默认启动内置 Mock。如需对接真实服务：

1. 复制 `.env.example` 为 `.env`，填写真实 `BASE_URL`、`TEST_USERNAME`、`TEST_PASSWORD`；
2. 在 `conftest.py` 中将 `mock_server` fixture 改为直接返回 `.env` 中的 `BASE_URL`
   （不再启动内置 Mock），或通过环境变量控制是否启动 Mock；
3. 调整 `mock_server/app.py` 的接口契约以匹配真实服务（或直接弃用 Mock）。

---

## 六、报告与日志

- **HTML 报告**：`reports/report.html`，单文件自包含，含用例标题、状态、耗时、环境信息。
- **日志**：`logs/autotest_YYYY-MM-DD.log`，按天轮转保留 7 天；控制台同步彩色输出。
- **报告钩子**：`conftest.py` 的 `pytest_configure` 注入环境元数据，
  `pytest_html_report_title` 自定义报告标题。

---

## 七、常见问题

**Q1：端口冲突怎么办？**
A：Mock 服务使用随机端口，不会冲突。如手动指定端口，确保未被占用。

**Q2：用例失败如何排查？**
A：查看控制台 loguru 日志与 `logs/` 文件；断言失败信息包含实际响应体；
HTML 报告展示用例状态与耗时。

**Q3：如何只跑某一类用例？**
A：使用 `-k` 按名称过滤，或 `-m` 按标记过滤（`login/articles/users/auth/smoke`）。

**Q4：pandas 未安装能否运行 CSV 用例？**
A：不能。`requirements.txt` 已包含 pandas，按文档安装即可。

**Q5：如何扩展 JSONPath？**
A：当前为轻量实现，复杂表达式可在 `utils/extract.py` 中引入 `jsonpath-ng` 扩展。

---

## 八、目录与文件清单

| 路径 | 说明 |
| --- | --- |
| `config/settings.py` | 配置读取 |
| `config/logging_conf.py` | 日志初始化 |
| `common/http_client.py` | HTTP 客户端封装 |
| `common/assertions.py` | 断言助手 |
| `common/auth.py` | 登录鉴权 |
| `common/logger.py` | 日志单例 |
| `utils/extract.py` | 数据提取 |
| `utils/generator.py` | 随机数据生成 |
| `utils/db_checker.py` | 数据库校验占位 |
| `mock_server/app.py` | FastAPI Mock 服务 |
| `testcases/test_login.py` | 登录用例 |
| `testcases/test_articles.py` | 文章用例 |
| `testcases/test_user.py` | 用户用例 |
| `data/*.yaml`、`data/*.csv` | 数据驱动文件 |
| `conftest.py` | 全局 fixture |
| `pytest.ini` | pytest 配置 |
| `run.py` | 运行入口 |
| `start.sh` / `start.bat` | 一键启动脚本 |
